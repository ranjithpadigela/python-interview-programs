I written the function which is countdown number of given input
firstly it creates the list of given number input and it checks the 
condition is greater than zero it follows the decreasing of number
at the end we will return List 